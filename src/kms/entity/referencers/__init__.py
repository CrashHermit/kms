"""Entity referencing: extract each entity's cross-entity references as open, LLM-named relations.

One pass (``open``) serves every entity channel — the three per-type referencers it replaced differed
only in the closed math vocabularies they enforced (see ``docs/GENERALIZATION.md``, step 2)."""
