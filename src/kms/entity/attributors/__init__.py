"""Entity attributors: fill the self-contained AutoMathKG attributes on a channel's entities.

``universal`` is the general one pass — label/number/title/contents plus the induced open ``type``;
``problem``/``definition``/``theorem`` are the validated per-type math path."""
