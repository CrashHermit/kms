{"stage": "seam_merger", "inputs": {"top_node_context": {"content": "We say that $G' = (V', E')$ is a **subgraph** of $G = (V, E)$, and write $G' \\subseteq G$, provided $V' \\subseteq V$ and $E' \\subseteq E$.\n\nWe say that $G' = (V', E')$ is an **induced subgraph** of $G = (V, E)$ provided $V' \\subseteq V$ and every edge in $E$ whose vertices are still in $V'$ is also an edge in $E'$.", "types": ["paragraph"]}, "top_bottom_edge_node": {"content": "Notice that every induced subgraph is also an ordinary subgraph, but not conversely. Think of a subgraph as the result of deleting some vertices and edges from the larger graph. For the subgraph to be an induced subgraph, we can still delete vertices, but now we only delete those edges that included the deleted vertices.", "types": ["paragraph"]}, "bottom_top_edge_node": {"content": "# **Example 2.1.7**", "types": ["header"]}, "bottom_node_context": {"content": "Consider the graphs:", "types": ["paragraph"]}}, "outputs": {"node": null}}
{"stage": "seam_merger", "inputs": {"top_node_context": {"content": "**Solution.** The number of vertices is easy to find. It is the number of degrees in the sequence: 7. To find the number of edges, we compute the degree sum", "types": ["paragraph"]}, "top_bottom_edge_node": {"content": "$$4 + 4 + 3 + 3 + 3 + 2 + 1 = 20,$$", "types": ["math"]}, "bottom_top_edge_node": {"content": "so the number of edges is half this: 10.", "types": ["paragraph"]}, "bottom_node_context": {"content": "The handshake lemma also tells us what is not possible.", "types": ["paragraph"]}}, "outputs": {"node": null}}
{"stage": "seam_merger", "inputs": {"top_node_context": {"content": "![2]()", "types": ["image"]}, "top_bottom_edge_node": {"content": "Vertices in a graph do not always have edges between them. If we add all possible edges, then the resulting graph is called **complete**. That is, a graph is complete", "types": ["paragraph"]}, "bottom_top_edge_node": {"content": "if every pair of vertices is connected by an edge. Since a graph is determined completely by which vertices are adjacent to which other vertices, there is only one complete graph with a given number of vertices. We give these a special name: $K_n$ is the complete graph on $n$ vertices.", "types": ["paragraph"]}, "bottom_node_context": {"content": "Each vertex in $K_n$ is adjacent to $n - 1$ other vertices. We call the number of edges emanating from a given vertex the **degree** of that vertex. So every vertex in $K_n$ has degree $n - 1$. How many edges does $K_n$ have? One might think the answer should be $n(n - 1)$, since we count $n - 1$ edges $n$ times (once for each vertex). However, each edge is incident to 2 vertices, so we counted every edge exactly twice. Thus there are $n(n - 1)/2$ edges in $K_n$. Alternatively, we can say there are $\\binom{n}{2}$ edges, since to draw an edge we must choose 2 of the $n$ vertices.", "types": ["paragraph"]}}, "outputs": {"node": {"content": "Vertices in a graph do not always have edges between them. If we add all possible edges, then the resulting graph is called **complete**. That is, a graph is complete if every pair of vertices is connected by an edge. Since a graph is determined completely by which vertices are adjacent to which other vertices, there is only one complete graph with a given number of vertices. We give these a special name: $K_n$ is the complete graph on $n$ vertices.", "types": ["paragraph"]}}}
